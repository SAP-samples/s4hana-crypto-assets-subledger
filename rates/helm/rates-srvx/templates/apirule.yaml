apiVersion: gateway.kyma-project.io/v1beta1
kind: APIRule
metadata:
  name: {{ .Values.services.srvx.name }}
spec:
  gateway: {{ .Values.gateway }}
  host: {{ .Values.services.srvx.name }}.{{ .Values.clusterDomain }}
  rules:
    - path: /.*
      accessStrategies:
        - config: {}
          handler: noop
      methods:
        - HEAD
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
  service:
    name: {{ .Values.services.srvx.name }}
    port: {{ .Values.services.srvx.service.port }}