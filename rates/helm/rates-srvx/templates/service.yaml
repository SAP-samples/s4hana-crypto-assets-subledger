apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.srvx.name }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
    app: {{ .Values.services.srvx.name }}
spec:
  ports:
    - name: http
      port: {{ .Values.services.srvx.service.port }}
      targetPort: {{ .Values.services.srvx.image.port }}
      protocol: TCP
  selector:
    app: {{ .Values.services.srvx.name }}