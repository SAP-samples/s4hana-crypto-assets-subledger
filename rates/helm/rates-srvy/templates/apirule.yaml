apiVersion: gateway.kyma-project.io/v1beta1
kind: APIRule
metadata:
  name: {{ .Values.services.srvy.name }}
spec:
  gateway: {{ .Values.gateway }}
  host: {{ .Values.services.srvy.name }}.{{ .Values.clusterDomain }}
  rules:
    - path: /.*
      accessStrategies:
        - config: {}
          handler: noop
      methods:
        - HEAD
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
  service:
    name: {{ .Values.services.srvy.name }}
    port: {{ .Values.services.srvy.service.port }}