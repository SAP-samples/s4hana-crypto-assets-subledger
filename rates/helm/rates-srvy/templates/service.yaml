apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.srvy.name }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
    app: {{ .Values.services.srvy.name }}
spec:
  ports:
    - name: http
      port: {{ .Values.services.srvy.service.port }}
      targetPort: {{ .Values.services.srvy.image.port }}
      protocol: TCP
  selector:
    app: {{ .Values.services.srvy.name }}