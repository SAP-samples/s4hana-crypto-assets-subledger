apiVersion: gateway.kyma-project.io/v1beta1
kind: APIRule
metadata:
  name: {{ .Values.services.xyox.name }}
spec:
  gateway: {{ .Values.gateway }}
  host: {{ .Values.services.xyox.name }}.{{ .Values.clusterDomain }}
  rules:
    - path: /.*
      accessStrategies:
        - config: {}
          handler: noop
      methods:
        - HEAD
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
  service:
    name: {{ .Values.services.xyox.name }}
    port: {{ .Values.services.xyox.service.port }}