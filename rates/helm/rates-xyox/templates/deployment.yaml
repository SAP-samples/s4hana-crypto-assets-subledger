apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.xyox.name }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
    app: {{ .Values.services.xyox.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.services.xyox.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.xyox.name }}
    spec:
      containers:
        - image: "{{ .Values.dockerID }}/{{ .Values.services.xyox.image.repository }}:{{ .Values.services.xyox.image.tag }}"
          name: {{ .Values.services.xyox.name }}
          imagePullPolicy: {{ .Values.services.xyox.image.pullPolicy }}
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
            requests:
              memory: 256Mi
              cpu: "0.2"
          ports:
            - name: http
              containerPort: {{ .Values.services.xyox.image.port }}
          envFrom:
            - configMapRef:
                name: {{ .Values.services.xyox.name }}
          env:
            - name: SERVICE_BINDING_ROOT
              value: /bindings
          volumeMounts:
            - name: rates-uaa
              mountPath: "/bindings/rates-uaa"
              readOnly: true
      volumes:
        - name: rates-uaa
          secret:
            secretName: {{ .Values.services.uaa.bindingSecretName }}
