apiVersion: gateway.kyma-project.io/v1beta1
kind: APIRule
metadata:
  name: {{ .Values.services.yyoy.name }}
spec:
  gateway: {{ .Values.gateway }}
  host: {{ .Values.services.yyoy.name }}.{{ .Values.clusterDomain }}
  rules:
    - path: /.*
      accessStrategies:
        - config: {}
          handler: noop
      methods:
        - HEAD
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
  service:
    name: {{ .Values.services.yyoy.name }}
    port: {{ .Values.services.yyoy.service.port }}