apiVersion: gateway.kyma-project.io/v1beta1
kind: APIRule
metadata:
  name: {{ .Values.services.zzoz.name }}
spec:
  gateway: {{ .Values.gateway }}
  host: {{ .Values.services.zzoz.name }}.{{ .Values.clusterDomain }}
  rules:
    - path: /.*
      accessStrategies:
        - config: {}
          handler: noop
      methods:
        - HEAD
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
  service:
    name: {{ .Values.services.zzoz.name }}
    port: {{ .Values.services.zzoz.service.port }}